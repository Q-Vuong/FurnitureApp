<!DOCTYPE html>
<html lang="en">

<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link href="/css/component/navigation.css" rel="stylesheet">
  <link href="/css/component/breadcrumb.css" rel="stylesheet">
  <link href="/css/pages/banner.css" rel="stylesheet">
  <style>
    /* Định dạng container để căn giữa nội dung */
    .container {
      display: flex;
      justify-content: center;
      /* Căn giữa theo chiều ngang */
      align-items: center;
      /* Căn giữa theo chiều dọc */
    }

    .card {
      padding-top: 10px;
      width: 550px;
      border-radius: 20px;
    }

    /* Định dạng ảnh trong card */
    .card-img-top {
      width: 200px;
      height: auto;
      display: block;
      /* Hiển thị hình ảnh là block element */
      margin: 0 auto;
    }

    #options {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
    }

    #options a {
      font-weight: bold;
      font-size: medium;
      padding: 10px;
      width: auto;
      height: 45px;
      align-items: center;
      border-radius: 20px;
      border: 1px solid rgb(196, 196, 196);
      box-shadow: 0px 0px 30px 1px rgb(196, 196, 196);
      margin-left: 25px;
      margin-right: 25px;
    }
  </style>
</head>

<body>

  <div class="container mt-3">
    <div class="card">
      <img class="card-img-top" src={{admin.avatar}} alt="">
      <div class="card-body">
        <div id="admin-template" type="text/x-handlebars-template">
          <p><strong>Tên:</strong> {{admin.fullName}}</p>
          <p><strong>Số điện thoại:</strong> {{admin.phoneNumber}}</p>
          <p><strong>Địa chỉ:</strong> {{admin.address}}</p>
          <p><strong>Giới tính:</strong> {{admin.gender}}</p>
          <p><strong>Ngày sinh:</strong> {{admin.birthDate}}</p>
          <div id="options">
            <a href="admin/viewbyid" class="btn btn-warning btn-sm eLink ">Chỉnh sửa</a>
            <a href="admin/changepassView" class="btn btn-primary btn-sm">Đổi mật khẩu</a>
            <a href="/" class="btn btn-danger btn-sm">Đăng xuất</a>
          </div>
        </div>

      </div>


    </div>
    <br>

    <script src="/js/admin/loadpage_ad.js"></script>

</body>


</html>